---
title: yt-dlp 下载
description: 通过 Web 或 Telegram Bot 创建媒体链接下载任务
---

{% include nav.html %}

# yt-dlp 下载

TG Vault 可从 **Web 文件页**或 **Telegram Bot** 创建 yt-dlp 任务。后端会安全校验链接，在独立临时目录下载/合并媒体，再写入提交任务时选定的存储目标。

## 在 Web 中使用

1. 登录 TG Vault，打开文件管理中的 **YT-DLP** 分类。
2. 在“添加下载任务”中粘贴 yt-dlp 支持站点的单个视频或音频页面链接。
3. 选择：
   - **视频（最佳质量）**
   - **仅音频（MP3）**
4. 点击 **添加下载任务**。
5. 前往 **任务中心**查看排队、下载、上传、失败、取消或重试状态。

Web 任务默认保存到当前系统存储源的 `ytdlp` 目录。播放列表默认不处理。

## 在 Telegram Bot 中使用

1. 先通过 `/start` 完成身份验证。
2. 如需改变目标，可先使用 `/target`；否则使用系统默认存储。
3. 发送：

```text
/ytdlp https://example.com/video
```

4. Bot 探测标题和来源后，选择 **最佳视频**、**仅音频**或 **取消**。
5. 使用 `/tasks` 或 Web 任务中心查看进度。

<div class="callout warning">
每次只接受一个以 <code>http://</code> 或 <code>https://</code> 开头的公网媒体页面链接。内网、本机、保留地址和播放列表会被拒绝。请只下载你有权保存的内容，不要绕过版权、付费访问或平台权限控制。
</div>

## 存储位置与目标

后端使用 `YTDLP_WORK_DIR` 作为临时工作目录，完成文件归类到 `ytdlp`：

```dotenv
YTDLP_BIN=yt-dlp
YTDLP_WORK_DIR=/data/uploads/ytdlp
YTDLP_MAX_CONCURRENT=1
```

Bot 任务的目标优先级：

1. 当前聊天的一次性 `/target once`
2. 当前聊天的 `/target session`
3. 系统默认存储

Web 任务使用提交时的系统默认存储。目标会在任务创建时形成快照；提交后再切换默认存储，不会改变已有任务。

官方后端构建提供 `yt-dlp` 与 `ffmpeg`。自定义镜像必须确保二者都可在容器 PATH 中执行。

## 执行流程

```text
用户提交单个 URL
  → 后端校验协议、目标地址和播放列表边界
  → 创建持久化任务并捕获存储目标
  → yt-dlp 在独立临时目录下载/合并
  → 写入目标存储并登记文件
  → 生成可用的预览信息
  → 清理临时目录并更新任务中心
```

失败不会伪装成成功。外部存储写入结果不明确时，任务会进入需要对账的状态，避免盲目重试造成重复文件。

## 常见问题

### 链接无法创建任务

可能原因包括：链接不是 HTTP(S)、指向本机/内网/保留地址、是播放列表、长度超过限制，或目标存储当前不可写/处于冷却状态。

### 链接无法下载

可能原因包括：平台不受当前 yt-dlp 版本支持、内容需要登录、内容已删除、地区限制或服务器无法访问源站。

```bash
docker compose logs --tail=250 backend
```

### 下载后不能预览

检查容器内 `ffmpeg`、媒体 MIME 类型、缩略图生成日志和浏览器支持格式。

### 任务一直等待或速度慢

在任务中心、`/tasks` 或 `/status` 查看：

- `YTDLP_MAX_CONCURRENT` 是否已被其他任务占用
- 目标存储是否冷却或连接失败
- 临时磁盘是否低于安全水位
- 源站、服务器网络、媒体合并或目标上传是否成为瓶颈

不要只靠提高并发解决问题，应先从任务阶段和日志判断瓶颈。

---

[返回文档中心](./) · [Telegram 配置](./telegram.html) · [工作原理](./architecture.html)
